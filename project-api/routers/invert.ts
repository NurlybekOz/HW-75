import express from "express";
const invertRouter = express.Router();

invertRouter.post('/encode', async (req, res) => {

});
invertRouter.post('/encode', async (req, res) => {
});

export default invertRouter;
